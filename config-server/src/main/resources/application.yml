server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mstao/spring-cloud-config
          searchPaths: /**
          label: master
          username:
          password:
